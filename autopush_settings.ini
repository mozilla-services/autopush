; Connection node settings.

[connection]
; The port on which to listen for WebSocket connections.
port = 8080

; Settings for the internal routing listener, used to deliver routed
; notifications to clients connected to this node.
router_hostname =
router_port = 8081
router_ssl_key =
router_ssl_cert =

; The endpoint hostname and port, used to construct the push endpoint
; URL for each registered channel.
endpoint_hostname =
endpoint_port = 8082
